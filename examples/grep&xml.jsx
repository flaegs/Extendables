#include "~/Dropbox/Stämpfli/Extendables/extendables.jsx"xmlEl = current('document').xmlElements[0].getXPath("//Titel_2")[1].grepStyle("Die", "Kursiv");// Get the second (= [1]) XML-Element with a Tag of "Titel_2". Finde all occurances of "Die" and give it the style "Kursiv"// It doesn't matter if "Kursiv" is a paragraphStyle or a characterStyle. But paragraphStyles take precedence over characterStlyes.// The function looks for the styles in app.activeDocument.// Cleaner and more secure use of the grepStyle-function is// to give it a Paragraph- or CharacterStyle as the second argumentxmlEl = current('document').xmlElements[0].getXPath("//Titel_2")[1].grepStyle("Die", current('document').characterStyles.item("Kursiv"));