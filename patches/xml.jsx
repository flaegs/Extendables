/** * @desc Finds all matching elements (for the given XPath-Expresssion) inside of an XMLElement-Object. * * @param {String} theXPath */ResultsGlobalWhichCouldNameClash730472 = []; // This is quite a hack, where I have to use a global variable for the results. XMLElement.prototype.getXPath = function (theXPath){		var that = this		var myRuleSet = new Array (new find);		with(that){			var elements = xmlElements;			__processRuleSet(elements.item(0), myRuleSet);		}	function find(){		this.name = "find";		this.xpath = theXPath;			this.apply = function(myElement, myRuleProcessor){			__skipChildren(myRuleProcessor);			ResultsGlobalWhichCouldNameClash730472.push(myElement); // Hack!!		}		return false	}  var results = ResultsGlobalWhichCouldNameClash730472.clone(); // Hack!!  ResultsGlobalWhichCouldNameClash730472 = []; // Hack!!  return results; }